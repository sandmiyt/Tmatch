# Tmatch 项目协作约定

- 本仓库是题竞 iOS 客户端。实际 Xcode Target 使用 `Tijing/`，不要误改外层旧副本。
- 用户已要求：此项目以后每次获准的代码更新，完成相应检查后默认提交并推送到 GitHub `sandmiyt/Tmatch`，除非用户本次另有要求。
- 推送前核对远端、当前分支和差异，只提交任务相关文件。发现远端分叉、权限错误或冲突时停止并说明，不强制推送、不覆盖其他改动。
- 不将网页版后端、数据库、上传内容、密钥或构建产物混入本仓库。不操作生产服务器。
- 保留原有功能。用户已移除学习周报、目标计划与补交中心页面；后台可靠补交仍保留，不要擅自重新添加这些页面。
- 静态检查不代表 Xcode 编译、单元测试或真机验证通过。报告时区分已执行检查和待验证项目。
