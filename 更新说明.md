# 题竞 iOS 底部导航遮挡修复 V2

> V1 的透明安全区方案在实际 `TabView` / `NavigationStack` 层级中没有可靠传递，并且关闭了旧页面占位。请不要再使用 V1，以本包为准。

## V2 修复

- 移除覆盖在页面上方的自定义底部导航栏实例。
- 把真实的自定义底部导航栏放进根容器的 `.safeAreaInset(edge: .bottom)`。
- 系统现在按导航栏实际 90pt 高度压缩当前页面的可用区域，而不是依赖各页面手工添加透明占位。
- 一级页面、历史战绩、通知中心、好友、账号安全、建议、条款、隐私及其他二级页面统一生效。
- 答题、每日挑战、匹配和对战房间隐藏底栏时，安全区域随底栏一起移除，不会保留空白。
- 原有一级页面的兼容占位保持 0 高度，避免双倍留白。

## 更新方法

将 `Tijing/App/RootView.swift` 覆盖到 Mac 上当前题竞工程的同名位置，然后在 Xcode 中 Clean Build Folder 后重新编译安装。不要同时使用 V1 文件。

## 验证范围

- 五个 Tab 接入检查：通过。
- 底栏唯一实例检查：通过。
- 底栏安全区域布局检查：通过。
- 底栏实际高度与原预留高度检查：`64 + 18 + 8 = 90pt`，通过。
- 全屏页面隐藏底栏检查：通过。
- Swift 花括号结构与 `git diff --check`：通过。
- 压缩包仅包含本次修改源码和本说明，不包含数据库、日志、密钥、上传内容、备份或依赖目录。

当前环境为 Windows，无法代替 macOS 的 Xcode、模拟器和真机验证。安装后请重点走查“我的 → 历史战绩”“我的 → 通知中心”，滚动到页面最底部；同时确认答题和对战房间仍为全屏且底部没有多余空白。
