# 题竞 iOS 底部导航遮挡修复 V3

V1、V2 的根级安全区域方案已确认在当前 `TabView` / `NavigationStack` 结构中不能可靠解决问题。请废弃前两个包，只使用本 V3 包，并一次性覆盖包内全部 10 个 Swift 文件。

## V3 实际处理

- 恢复五个一级 Tab 原有且已有效的 90pt 页面底部占位，修复 V1/V2 导致的一级页面空白消失。
- 新增二级页面专用 `tijingTabBarPageClearance()` 修饰器。
- 该修饰器直接在当前页面本身执行 `.padding(.bottom, 90)`，不依赖 `TabView`、`NavigationStack` 的安全区域传播，也不依赖父页面环境值。
- 已直接应用到以下页面：
  - 历史战绩
  - 历史战绩对局详情
  - 通知中心
  - 好友与黑名单
  - 学习诊断
  - 考试日历
  - 编辑个人资料
  - 账号与安全
  - 功能建议
  - 使用条款与隐私政策共用页面
- 答题、每日挑战、匹配和对战房间仍使用全屏隐藏底栏逻辑，没有增加 90pt 空白。

## 更新要求

必须把压缩包内全部文件按照原目录一次性覆盖到当前 Xcode 工程。只覆盖 `RootView.swift` 会因为其他页面没有调用新修饰器而无效。

建议覆盖后在 Xcode 执行：

1. Product → Clean Build Folder。
2. 删除手机或模拟器上的旧 App。
3. 重新 Build 并安装。
4. 检查首页和“我的”一级页底部空白已恢复。
5. 检查历史战绩、战绩详情、通知中心等页面最底部内容与导航栏之间存在 90pt 避让。

## 静态验证

- 10 个二级/三级页面调用点检查：通过。
- 5 个一级页面 footer 检查：通过。
- 5 个全屏隐藏底栏入口检查：通过。
- 修改的 10 个文件均在 Xcode Sources 中：通过。
- Swift 花括号结构检查：通过。
- `git diff --check`：通过。

当前为 Windows 修改环境，无法冒充 macOS Xcode、模拟器或真机运行结果。
