# 题竞 iOS 二级页面底部导航遮挡修复

## 更新内容

- 将自定义底部导航栏的内容避让从“一级页面手工添加占位”改为 `NavigationStack` 统一的底部安全区域。
- 修复历史战绩、通知中心、好友与黑名单、账号与安全、功能建议、使用条款、隐私政策等二级页面底部内容被导航栏遮挡的问题。
- 同一机制覆盖首页、刷题、对战、排行和我的五个 Tab 内现有及后续新增的普通二级页面。
- 保留答题、每日挑战、匹配过程和对战房间等全屏页面隐藏底部导航栏时不预留空白的行为。
- 原有一级页面兼容占位自动降为 0，避免出现双倍底部空白。

## 覆盖方式

把压缩包中的 `Tijing/App/RootView.swift` 覆盖到服务器或 Mac 构建工程的同名路径，随后在 Xcode 中重新编译安装。

## 验证说明

- 已完成五个 Tab 全局接入、二级导航入口、旧占位重复留白和全屏隐藏状态的静态回归检查。
- 已通过 `git diff --check`。
- 当前修改环境为 Windows，无法代替 macOS 上的 Xcode 编译、模拟器和真机界面验证；发布前请在 Xcode 中至少走查“我的 → 历史战绩/通知中心”页面底部，以及答题/对战全屏页面。

## 文件范围

- 仅修改：`Tijing/App/RootView.swift`
- 不包含数据库、日志、上传文件、密钥、备份、依赖目录或服务器运行数据。
