# 题竞 iOS 子页面底部导航白框修复 V4

## 问题原因

V3 将 90pt 避让加在子页面最外层，导致页面背景也被一起缩短，底部露出了系统默认背景，因此在悬浮导航栏周围出现白框。

## V4 修改

- 删除全部子页面外层的 `tijingTabBarPageClearance()` 和外层 `.padding(.bottom, 90)`。
- 与首页保持相同方式：把 `TijingTabBarContentFooter()` 放进滚动内容的末尾。
- `List` 页面使用专用 `TijingTabBarListFooter()`：
  - 行背景透明；
  - 隐藏分割线；
  - 行边距为 0；
  - 不响应点击。
- 页面背景现在会继续铺满导航栏下方，不再露出白色矩形区域。
- 内容末尾仍保留 90pt 可滚动空间，最后一项不会被底部导航栏遮挡。

## 覆盖页面

- 历史战绩及对局详情
- 通知中心
- 好友与黑名单
- 学习诊断
- 考试日历
- 编辑个人资料
- 账号与安全
- 功能建议
- 使用条款和隐私政策

## 更新方式

必须一次性覆盖压缩包内全部 10 个 Swift 文件。覆盖后在 Xcode 执行 Clean Build Folder，删除设备上的旧 App，再重新编译安装。

## 验证

- 15 个保留底栏页面的内容 footer 检查：通过（5 个一级页面 + 10 个子页面）。
- 3 个 List 透明 footer 样式检查：通过。
- V3 外层 padding 已全部移除：通过。
- 5 个全屏页面隐藏底栏逻辑保留：通过。
- 10 个修改文件均属于 Xcode Target Sources：通过。
- Swift 结构检查及 `git diff --check`：通过。

当前为 Windows 修改环境，最终视觉效果仍需在 macOS Xcode、模拟器或真机确认。
